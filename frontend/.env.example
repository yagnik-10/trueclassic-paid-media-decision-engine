# VITE_API_BASE: Base URL of the FastAPI decision engine. Defaults to
# http://localhost:8000 when unset. The dev server is pinned to :3000 to match
# the backend CORS allowlist (see vite.config.ts).
VITE_API_BASE="http://localhost:8000"
