"""Stage 1 — thin end-to-end API shell (FastAPI).

Serves one *fixed* (placeholder) recommendation built from the deterministic
canonical dataset and records approve/reject decisions into a stubbed audit.
No modeling, optimizer, or live execution lives here — that is Stage 3+.
"""
